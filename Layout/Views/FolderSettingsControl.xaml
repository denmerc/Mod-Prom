<UserControl x:Class="Layout.Views.FolderSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="0 40 0 0">
        <Label Foreground="White"> Folders:</Label>
        <ListBox x:Name="FolderListBox" Margin="0 10 0 0" ItemsSource="{Binding}" Height="Auto" Width="Auto" Background="Transparent">
            <ListBox.ItemTemplate>
                <DataTemplate>

                    <StackPanel>
                        <TextBlock FontSize="16" Foreground="{StaticResource Foreground}"><Run Text="{Binding Name}"/></TextBlock>
                    </StackPanel>

                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

    </Grid>
</UserControl>
